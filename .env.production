# Production Environment Variables for Netlify
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Database Connection (Replace with your actual values in Netlify dashboard)
# DO NOT put actual credentials here - use Netlify environment variables
DATABASE_URL=postgresql://placeholder:placeholder@placeholder.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1&pool_timeout=20
DIRECT_URL=postgresql://placeholder:placeholder@placeholder.supabase.co:5432/postgres?sslmode=require

# NextAuth Configuration
NEXTAUTH_URL=https://your-netlify-url.netlify.app
NEXTAUTH_SECRET=YOUR_SECRET_HERE

# Database Connection Pooling
POSTGRES_POOL_MIN=1
POSTGRES_POOL_MAX=10
POSTGRES_IDLE_TIMEOUT=10000

# Prisma Configuration
PRISMA_CLIENT_ENGINE_TYPE=library

# This file should not contain actual secrets - use Netlify environment variables instead
# The actual values should be configured in Netlify's dashboard
